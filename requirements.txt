# NaviRAG 依赖
# Python 3.10+；torch 安装 CPU 版即可：
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

sentence-transformers>=3.0
chromadb>=0.5
torch>=2.0          # CPU 版即可，见上方注释
numpy
requests

# 以下为后续 UI 阶段依赖（已装可忽略）
streamlit>=1.36
pyyaml

# 桌面端（desktop_chat.py，二选一即可，网页版用 streamlit）
PySide6>=6.6
